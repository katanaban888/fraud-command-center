# Frontend

Next.js dashboard shell for Fraud Command Center. The browser uses relative `/api` URLs; `next.config.mjs` proxies those requests to `BACKEND_URL` during local development and deployment.
